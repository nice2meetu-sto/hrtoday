# 인원현황 대시보드 — 개발 인수인계 문서

> 서울관광재단 인원현황 대시보드. 클라우드(Claude Code 웹) → 로컬 개발 전환용 인수인계.
> 이 문서만 읽으면 구조·규칙·이력·주의점을 파악하고 이어서 개발할 수 있게 정리함.

---

## 1. 프로젝트 개요

- **무엇**: 그 날짜(기준일)의 인원·제도 현황을 한눈에 보는 **단일 페이지 HR 대시보드**. + 인원을 조직도로 보는 탭.
- **핵심 성격**: 데이터는 **구글시트**에 있고, **Apps Script(GET 전용 JSON API)** 로 읽어와 화면에서 계산·표시. **읽기 전용**(대시보드에서 시트를 수정하지 않음).
- **호스팅**: 정적 호스팅(GitHub Pages 등). 저장소 `nice2meetu-sto/hrtoday`.
- **연결 페이지**: 타이틀 옆 `인력현황표` 링크 → 같은 폴더의 **`hrhistory.html`**(별도 페이지, 이 저장소 밖에서 관리 중). 두 페이지를 하나처럼 오가게 함.

---

## 2. 기술 스택 / 절대 지켜야 할 제약  ⚠️

1. **단일 `index.html` + 바닐라 JS.** 프레임워크·빌드 도구·npm 없음. (파일 하나로 완결)
2. **데이터는 GET `fetch()` 만.** `google.script.run`·`HtmlService`·POST 금지 (GitHub Pages CORS 회피).
3. **브라우저 저장소 사용 금지** (localStorage 등). 상태는 메모리에만.
4. **폰트: Noto Sans KR, weight `400;500;700;900` 전부 로드.** 타이틀이 900을 씀 → **900 빠지면 두께가 달라짐.**
5. **시트 데이터는 "컬럼 이름"으로 접근.** 위치(순서) 기준 아님 → 시트 컬럼 순서 바꿔도 됨, **이름 바꾸면 코드 수정 필요.**
6. **날짜는 로컬/KST 기준.** `new Date().toISOString()`(UTC) 쓰지 말 것 → 날짜 하루 밀림.
7. 수정 후 **콘솔 에러 0, 모바일/아이패드 레이아웃 확인.**

---

## 3. 파일 구성

| 파일 | 설명 |
|---|---|
| `index.html` | **대시보드 본체** (전부 이 안에 있음: HTML·CSS·JS) |
| `Code.gs` | 구글시트에 붙이는 Apps Script (GET JSON API). ※ 저장소본은 구버전(인원·제도만) — 실제 배포본은 `팀 순서`·`정원`도 읽음 |
| `README.md` | 설치·배포·점검 가이드 |
| `정현원현황_표.html` | **독립 실행형** 정현원(정원·현원·결원) 표 — 다른 페이지 이식용 |
| `조직도_독립형.html` | **독립 실행형** 조직도 (시뮬레이션·'사용 중 제도' 코드 제외) — 이식용 |
| `개발_인수인계.md` | 이 문서 |

> `hrhistory.html`(인력현황표)은 이 저장소에 없음 — 별도 페이지로 같은 폴더에 배포.

---

## 4. 데이터 모델 (구글시트)

Apps Script가 **각 시트 1행(머리글)을 키로** 객체 배열을 만들어 반환.
반환 형식: `{ ok:true, data:{ 인원:[...], 제도:[...], '팀 순서':[...], 정원:[...] } }`

**인원 시트** (1인 1행)
`사번 · 사원명 · 부서 · 직급 · 고용형태 · 성별 · 입사일 · 퇴사일 · 근무지 · 직위 · 대체 · 비고`
- `직위`: 팀장/팀장대행/본부장/총괄매니저/팀원 등 (정렬·배지에 사용)
- `대체`: 육아휴직 등 대체로 뽑은 **기간제 행에, 대체 대상 휴직자의 "사번"** 입력 (조직도에서 사번→이름 자동 변환)
- `근무지`: 본사/센터명 등 (조직도 하위그룹, 본사는 라벨 생략)
- `비고`: `별도직군` 문구 포함 시 별도직군 분류(정현원표), 퇴사/입사예정 명단에 표시

**제도 시트** (제도 1건당 1행)
`사번 · 제도 · 시작일 · 종료일 · 상세 · 비고`
- `상세`: 단축 시간 등(예 `10:00-17:00`)

**팀 순서 시트**: `부서 · 표시순서 · 본부`  (직속 팀은 본부 비우거나 `직속`)

**정원 시트**: 직급/구분별 정원 (가로형·세로형 모두 파서 지원). ※ 현재 대시보드 화면엔 미사용(아래 8-이력 참고), `정현원현황_표.html`에서 사용.

- 날짜는 **`YYYY-MM-DD` 텍스트**. `퇴사일`·`종료일` 빈칸 가능. (`D()` 파서가 `2026.5.1`·`2026/5/1` 등도 처리)

---

## 5. 핵심 파생 규칙 (코드에서 자주 나오는 개념)

- **기준일(REF) vs 조회일**:
  - 대부분 탭 = 헤더의 **기준일(REF, 날짜선택기)** 기준.
  - **조직도 탭만 = 오늘(KST, `kstDate()`) 고정.**
- **`isActive(p, ref)`** (재직 판정): `입사일 ≤ ref` 그리고 (`퇴사일` 없음 또는 `ref ≤ 퇴사일`). → 퇴사일은 **마지막 근무일**.
- **`statusOf(p, ref)`**: `입사예정`(입사일>ref) / `퇴사`(ref>퇴사일) / `휴직`(휴직 분류 제도 현재) / `재직`.
  - ⚠️ **산전후휴가는 '휴가'라서 휴직에 안 들어감**(재직으로 집계).
- **`programState(pr, ref)`**: `시작일 ≤ ref ≤ 종료일`→현재 / `시작일>ref`→예정 / `ref>종료일`→종료.
  - **빈 시작일 → '예정'** 으로 간주.
- **`categoryOf(제도명)`**: 이름에 `단축`·`육아시간`→단축 / `휴가`·`산전후`→휴가 / `휴직`→휴직 / 그 외→기타. **파견은 제도명에 `파견` 포함으로 판정.**
- **`D(s)`**: 여러 날짜표기 허용 파서(빈칸·해석불가→null). **날짜 비교는 항상 이걸로.**
- **KST 날짜**: `ymd()`(로컬 YYYY-MM-DD), `today()`(로컬 자정), `kstYMD()`/`kstDate()`(Asia/Seoul).

---

## 6. 탭 구성 (7개)

DOM 순서와 무관하게 `tab(i)` → `TAB_PANELS` 매핑으로 표시:
`const TAB_PANELS=['p0','pStaff','p2','pLeave','pUpcoming','p1','p3'];`

| # | 탭 | 패널 | 기준 | 요약 |
|---|---|---|---|---|
| 0 | 기준일 현황 | p0 | REF | 상단 요약(총원/현원/휴직/파견) + KPI + 월 캘린더 + D-day |
| 1 | 조직도 | pStaff | 오늘(KST) | 대표이사→직속→본부별 팀 카드, 팀원 칩, 시뮬레이션 |
| 2 | 전체 명단 | p2 | REF | 표(검색·필터·정렬·CSV), 현원 기본 필터 |
| 3 | 휴직자 | pLeave | REF | 유형별 통계 박스 + 명단 |
| 4 | 예정자 | pUpcoming | REF | 도래 일정 목록(30/60/90일 필터) |
| 5 | 제도별 조회 | p1 | REF | 대분류 박스 + 세부 칩, 사용중/예정 |
| 6 | 타임라인 | p3 | REF | 제도 간트(기준일 -3~+12개월) |

**정렬 순서 규칙(예정/캘린더/D-day 공통)**: 날짜 → 유형 우선순위(퇴사→휴직시작→휴가시작→복직→단축→입사) → 사번. (`evSecondary`)

---

## 7. 조직도 탭 상세 (가장 많이 손댄 부분)

- **배치**: `대표이사`(상단) → `직속`(본부 없는 팀: 홍보팀·감사관) → **본부별 컬럼**(팀 순서 시트의 `본부`로 그룹). 본부 라벨은 표시 안 함. **대표이사·본부장 카드는 보라 테두리.**
- **팀 카드**: 헤더(팀명+인원수) 클릭 → **구성원 명단 모달**. 내부는 **근무지별 하위그룹**(본사는 라벨 생략).
- **인원 칩**: `이름 + 직급` (+직위 배지, +대체 이름). 클릭 → **개인 상세 팝업**(이름·직위/직급·고용형태·입사일·대체). 기간제=노랑.
- **칩 정렬(`orgMemberSort`)**: **직위 우선순위(`POS_RANK = {본부장:0, 팀장:1, 팀장대행:1, 총괄매니저:2}`)** → 직급순 → 입사일순. (직책 추가는 이 표에 숫자만 넣으면 됨)
- **인사배치 시뮬레이션**(`SIM_*` 상태, 메모리 전용):
  - `이름 선택 → 팀 탭`: 팀 이동 + 근무지 본사 리셋
  - `이름 선택 → 근무지 탭`: 그 근무지·팀으로
  - `근무지 선택(아무도 안 골랐을 때 근무지 탭) → 팀 탭`: 그 근무지 전원 팀 이동
  - **결과 CSV(전체 명단 + 부서·근무지)**, **종료 시 원복.** 시트 저장 안 함.

### 칩 배경/테두리 색상값 (다른 곳에도 재사용)
| 구분 | 배경 | 테두리 |
|---|---|---|
| 기본 | `#f4f5f8` | `1px solid #e2e4ea` |
| 기간제 | `#fdf3cf` | `#efe1a6` |
| 단축 | `#dbe8fb` | `#bcd3f4` |
| 휴가 | `#fbe4cf` | `#f0c9a3` |
| 휴직 | `#eaeaec` | 기본(`#e2e4ea`) |
| 파견 | `#eaeaec` | `1.5px solid #8b909c` |

> ※ 조직도 독립형(`조직도_독립형.html`)에는 시뮬·제도색이 빠져 있어 **기간제 색만** 적용됨.

---

## 8. 개발 이력 (요약 — 무엇을 왜 만들었나)

시간순 주요 변경:
1. **초기 대시보드** 구축(4탭: 기준일 현황·제도별 조회·전체 명단·타임라인), Apps Script 연동, 샘플 데이터 폴백.
2. **날짜 처리 견고화**: 빈칸/다양한 표기 대응(`D()`), null 가드.
3. **기준일 현황**: KPI 개편, D-day 7개 페이지네이션, 캘린더 `+N` 호버 팝오버.
4. **전체 명단**: 전체검색(제도·날짜 포함, '현재 사용 제도'는 기준일 현재만), 정렬(사번·직급·입사일·퇴사일), 기간 필터, `현원`(재직+휴직) 기본 필터, 퇴사·입사예정 시 `비고` 컬럼.
5. **탭 추가**: 휴직자·예정자. (산전후휴가는 휴직에서 제외로 정정)
6. **KST 날짜** 정정(UTC 밀림 수정), 기준일↔캘린더 월 연동, 날짜입력 연도 4자리 자동진행(`max=2100-12-31`).
7. **인원현황표 탭**(직급별·정현원 표) → 이후 첨부 디자인으로 직급별 표 개편(성별 분할·파스텔·십자호버·부서모달) →
   **최종적으로 이 탭을 '조직도'로 전면 교체**(통계 표 제거). ⇒ **그 잔재로 `renderStaffStats/renderRankTable/renderQuotaTable/exportStaffStats` 및 `QUOTA` 로드가 코드에 남아있으나 호출 안 됨(무해).**
8. **조직도** 반복 개선: 본부 계층(팀 순서 시트 `본부`), 근무지 하위그룹, 직위 배지·정렬, 대체 표시, 대표/본부장 테두리, 개인 팝업, 팀 모달(상태→현재 제도), 색·정렬 미세조정.
9. **인사배치 시뮬레이션** 추가(탭-투-무브 + PC 드래그, CSV, 원복).
10. 헤더: **DB 버튼**(비밀번호 `stohr` 후 구글시트 열기), **인력현황표 링크**(→ `hrhistory.html`).
11. 전역 호버 효과, 표 행 음영 등 스타일 다듬기.

> ⚠️ 개발 중 **되돌린(Revert) 기능**: 상단 요약 바(→이후 재도입), 정현원 비교표, 센터별 근무자 현황, 정원 **변경일 버전관리**. 즉 **정원 변경일 기반 로직은 현재 없음**(정현원현황_표.html도 단일 스냅샷).

---

## 9. 자주 밟는 함정 (반드시 기억) 🧨

- **iOS Safari 버튼 글자색**: `<button>`에 `color` 명시 안 하면 텍스트가 **파란색**으로 나옴 → `color`와 `-webkit-appearance:none` 지정.
- **폰트 900 미로드** → 굵기 달라 보임(faux-bold). 폰트 링크에 `;900` 필수.
- **UTC 날짜** → 하루 밀림. 로컬/KST 유틸만 사용.
- **컬럼 rename** → 코드가 이름으로 찾으므로 깨짐(순서 변경은 안전).
- **CSV 한글 깨짐** 방지: 앞에 BOM `﻿` 붙임(이미 `dl()`이 처리).
- **DB 비밀번호(`stohr`)는 진짜 보안 아님**(소스에 노출). 민감 데이터면 시트 공유권한으로 막아야 함.
- **미사용 함수 존재**(8-7 참고): 지우려면 확인 후. 안 지워도 무해.

---

## 10. 로컬 개발 방법

- **실행**: `index.html`을 브라우저로 그냥 열면 됨(빌드 없음). 단, `fetch()`가 로컬파일에서 CORS 걸릴 수 있으니 간단 서버 권장:
  ```
  python3 -m http.server 8000   # http://localhost:8000/index.html
  ```
- **실데이터 없이 보기**: 코드 상단 `const SCRIPT_URL=""` 로 비우면 **내장 샘플 데이터**로 뜸(현재는 실제 배포 URL이 들어가 있음).
- **JS 문법 빠른 점검**(선택): `<script>` 내용만 뽑아 `node --check` 하면 오타 잡힘.
- **배포**: 파일 저장 → GitHub Pages면 push. Apps Script는 시트 열 추가/변경 없으면 재배포 불필요(코드 바꿨을 때만 새 배포).

---

## 11. Apps Script(Code.gs) / 배포 메모

- `SHEET_NAMES`에 **읽을 시트만** 나열 → **그 시트만 읽음**(다른 시트 있어도 로딩 안 느려짐).
- 배포: `배포 > 새 배포 > 웹 앱`, 실행=나, 액세스=**모든 사용자**. 나온 URL을 `index.html`의 `SCRIPT_URL`에 넣음.
- **로딩 느림 원인**: ① Apps Script cold start(첫 호출 1~3초) ② 읽는 시트의 행×열 양. (안 읽는 시트는 무관)
- 날짜 컬럼은 시트에서 **텍스트 서식 `YYYY-MM-DD`** 권장(`getDisplayValues()`로 보이는 그대로 내보냄).

---

## 12. 디자인 토큰(:root) — 색 통일용

```
--main:#534AB7  --main-soft:#ece9fb  --main-line:#cfc8f2
--valid:#3B6D11  --warn:#A32D2D  --amber:#b8860b
--border:#e2e4ea  --ink:#2a2c33  --ink2:#6b6f7a  --bg:#f5f6f8  --card:#ffffff
제도 대분류: 단축#5b9bd5 · 휴가#e09a6b · 휴직#8b80e0 · 기타#9aa0ad
```

---

### 한 줄 요약
**단일 HTML + 바닐라 JS, 구글시트를 GET JSON으로 읽어 계산·표시하는 읽기전용 HR 대시보드.** 데이터는 컬럼 "이름"으로 접근, 날짜는 KST, 조직도만 오늘 기준·나머지는 기준일. 위 함정만 피하면 안전하게 이어서 개발 가능.
